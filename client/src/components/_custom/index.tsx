/**
 * Centralized custom component
 * All the related custom component must be import
 */

import CustomDialog from "./CustomDialog";
import CustomTable from "./CustomTable";

export { CustomDialog, CustomTable };
